dependencies:
  pre:
    - gem install fpm --no-ri --no-rdoc 
    - printf '[url "git@github.com:"]\n\tinsteadOf = https://github.com/\n' > ~/.gitconfig
 
deployment:
  staging:
    branch: cli-tuning
    commands:
      - ./build.sh
